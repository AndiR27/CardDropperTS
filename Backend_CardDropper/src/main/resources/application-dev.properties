# Dev profile — disables Keycloak authentication
# Run with: SPRING_PROFILES_ACTIVE=dev ./mvnw spring-boot:run

# Disable OAuth2 resource server auto-config
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration